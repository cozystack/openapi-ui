import styled from 'styled-components'

const Grid = styled.div`
  display: grid;
  grid-gap: 72px;
  grid-template-columns: repeat(3, 1fr);
  height: 100%;
  min-height: 80vh;
`

export const Styled = {
  Grid,
}
